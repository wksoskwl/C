#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>

int main()
{
	int a, b;
	scanf("%d %d", &a, &b);
	for (int i = a; i >= 0; i--)
	{
		if (b % i == 0 && a % i == 0)
		{
			printf("%d", i);
			break;
		}
	}
}
