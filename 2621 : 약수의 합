#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>

int main()
{
	int N;
	scanf("%d", &N);
	int i,j,cnt = 0,sum = 0;
	for (i = 1; i <= N; i++)
	{
		if (N % i == 0)
		{
			sum += i;
		}
	}
	printf("%d", sum);
}
