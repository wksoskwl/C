#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>

int main()
{
	int i, a, b, c;
	scanf("%d %d %d", &a, &b, &c);
	for (i = a; i >= 1; i--)
	{
		if (a % i == 0 && b % i == 0 && c % i == 0)
		{
			printf("%d", i);
			break;
		}
	}
}
