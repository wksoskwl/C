#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>

int main()
{
    int y, m, d;
    scanf("%d %d %d", &y, &m, &d);
    if ((y - m + d) % 10)
    {
        printf("그럭저럭");
    }
    else
    {
        printf("대박");
    }
}
